import React from "react";

const Logout = () => {
	return (
		<div>
			<h1>Logout</h1>
			Logout button will be implemented here. It will clear the user
			session and redirect to the login page.
		</div>
	);
};

export default Logout;
